import React from "react";

const Navbar = () => (
  <nav className="navbar navbar-dark bg-primary mb-3">
    <div className="container">
      <span className="navbar-brand" style={{ height: '50px' }}>
        <h1>Git Profile App</h1>
      </span>
    </div>
  </nav>
);

export default Navbar;
